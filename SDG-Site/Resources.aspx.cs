using System;

namespace SDG_Site {
	public partial class Resources : System.Web.UI.Page {
		protected void Page_Load(object sender, EventArgs e) {
			/*
			// Number of postings
			int totalCount = GetPostsCount(0);

			// Number of postings to float on one page
			const int listCount = 10;
			// Number of pages to float
			const int pageCount = 10;

			// Number of pages
			int totalPage = totalCount / listCount;
			if (totalCount % listCount > 0)
				totalPage++;

			// If the requested page is more than the total page
			if (page > totalPage)
				page = totalPage;

			int startPage = ((page - 1) / pageCount) * pageCount + 1;
			int endPage = startPage + pageCount - 1;

			// If the end page is more than the total page
			if (endPage > totalPage)
				endPage = totalPage;*/
		}
	}
}